import QtQuick
import qs.Ui

// Placeholder: the pill arrives with SNG-2. It exists so the manifest validates and loads.
BarWidget {
  id: root
  moduleName: "io.github.igorkramar.singularity"
  implicitWidth: 0
  implicitHeight: 0
}

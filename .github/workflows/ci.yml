name: ci

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Manifest is valid JSON
        run: python3 -c 'import json; m = json.load(open("manifest.json")); print(m["id"], m["version"], m["kinds"])'
